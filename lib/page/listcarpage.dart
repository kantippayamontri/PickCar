import 'package:flutter/material.dart';

class ListCarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ListCarPage"),
      ),
    );
  }
}